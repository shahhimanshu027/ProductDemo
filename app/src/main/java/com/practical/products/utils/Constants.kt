package com.practical.products.utils

/**
 * application's constant file
 * all the const parameter should be declared here
 */
object Constants {
    const val PRODUCT_DATABASE = "ProductDatabase.db"
    const val CALL_TIMEOUT = 30L
    const val CONNECT_TIMEOUT = 30L
    const val READ_TIMEOUT = 30L
    const val WRITE_TIMEOUT = 30L
    const val SPLASH_DELAY = 1000L
}

object EndPoints {
    const val GET_PRODUCT_LIST = "products"
}
